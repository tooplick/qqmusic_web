# 使用 Python 3.11 官方镜像
FROM python:3.11-slim

# 设置工作目录
WORKDIR /app

# 复制依赖文件
COPY requirements.txt .

# 安装 Python 依赖
RUN pip install -r requirements.txt

# 复制所有项目文件
COPY . .

# 暴露端口
EXPOSE 6022

# 设置启动命令
CMD ["python", "app.py"]